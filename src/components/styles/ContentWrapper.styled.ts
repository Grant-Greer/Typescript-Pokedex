import styled from 'styled-components';

export const StyledContentWrapper = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 0;
    gap: 8rem;
    width: calc(919 / 1512);
    height: calc(696 / 1512);
    position: absolute;
    left: calc(297 / 1512);
    margin-top: 4rem;
    background-color: #fefefe;
`;
